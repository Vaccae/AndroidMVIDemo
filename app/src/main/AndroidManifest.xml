<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pers.vaccae.mvidemo">

    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <meta-data android:name="android.supports_size_changes" android:value="true" />
    <meta-data
        android:name="android.allow_multiple_resumed_activities"
        android:value="true" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVIDemo">
        <activity
            android:name=".ui.view.WindowInfoTrackerActivity"
            android:exported="true" />
        <activity
            android:name=".ui.view.DemoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.view.FoldActivity"
            android:exported="true" />
        <activity
            android:name=".ui.view.MainActivity"
            android:exported="true" />
    </application>

</manifest>